/*
Controls the vehicle to drive autonoumously throughout the city using pretrained model and data
*/
using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class AutonomousMode : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
